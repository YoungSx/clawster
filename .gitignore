# Secrets and sensitive configuration - NEVER commit these
# Use config.example.json as template (tracked in git), copy to config.json (gitignored)
config/secrets.json
config/config.json
*.key
*.pem
*.der
.env
.env.*
secrets/

# Doppler
.doppler/

# AWS/R2 credentials
.aws/
~/.aws/
aws_credentials*
r2_config*

# Any file containing hostnames, endpoints, tokens
# These should always be loaded from environment/Doppler
*_endpoint*
*_host*
*_token*
*_api_key*
*bot_id*

# Logs
logs/*.log
logs/*.log.*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# IDE
.vscode/
.idea/
*.swp
*.swo
.claude/

# OS
.DS_Store
Thumbs.db
*.bak